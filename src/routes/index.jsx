import { Route, Routes, BrowserRouter } from "react-router-dom";

const Routers = () => {
  <BrowserRouter>
    <Routes>
      <Route></Route>
    </Routes>
  </BrowserRouter>;
};

export default Routers;
